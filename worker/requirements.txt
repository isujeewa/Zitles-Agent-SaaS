# Claude Agent SDK
claude-agent-sdk

# Direct Claude API client (used inside zitles-report-tools for parallel extraction)
anthropic

# AWS — for uploading outputs to S3 at task exit
boto3

# Skills runtime deps
pdfkit
jinja2
PyPDF2
python-docx
google-cloud-vision
PyMuPDF
